<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.che300.objcache">

    <application>
        <provider
            android:name=".ContextProvider"
            android:authorities="${applicationId}.objcache.ContextProvider" />
    </application>
</manifest>